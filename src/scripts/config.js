/**
 * A global object that can be used in any of the modules to reference global application config options. This object was created in order to make the process of branching and merging in GIT alot easier.
 * @type object | config
 */
var config = {
    // web || exh
    type: 'exh'
};